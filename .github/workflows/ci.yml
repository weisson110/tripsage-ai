name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
    paths:
      - 'src/**'
      - 'e2e/**'
      - 'public/**'
      - 'scripts/**'
      - 'docs/**'
      - '.github/workflows/**'
      - '.pre-commit-config.yaml'
      # Root config files (post-flattening)
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'tsconfig.json'
      - 'vitest.config.ts'
      - 'biome.json'
      - 'next.config.ts'
      - 'tailwind.config.mjs'
      - 'postcss.config.mjs'
      - 'playwright.config.ts'
      - 'vercel.json'
      - 'components.json'
      - '.nvmrc'
      - 'Dockerfile*'
      - 'README.md'
      - 'CHANGELOG.md'
  workflow_dispatch: {}

permissions:
  contents: read
  pull-requests: read

env:
  COREPACK_VERSION: "0.35.0"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend (lint, type, unit - quick)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Fetch main for diff checks
        run: git fetch --no-tags origin main

      - name: Set up Node
        uses: actions/setup-node@v7.0.0
        with:
          node-version-file: '.nvmrc'

      - name: Install deps
        run: |
          npm install -g "corepack@${COREPACK_VERSION}"
          corepack enable pnpm
          corepack install
          pnpm ci

      - name: Cache Vite/Vitest/TS caches
        uses: ./.github/actions/cache-vite-vitest

      - name: Biome fix (must be clean)
        run: pnpm biome:fix

      - name: Fail if Biome introduced diffs
        run: git diff --exit-code

      - name: Validate active documentation links
        run: pnpm docs:check

      - name: TypeScript type check
        run: pnpm type-check

      - name: Reject unknown casts (full scan)
        run: pnpm check:no-unknown-casts

      - name: Reject new unknown casts
        run: pnpm check:no-new-unknown-casts

      - name: Reject @fileoverview drift (changed files)
        run: pnpm check:fileoverviews

      - name: Enforce Zod v4 style (changed files)
        run: pnpm check:zod-v4

      - name: Enforce OTEL dependency convergence
        run: pnpm check:otel-convergence

      - name: Enforce API route error helpers (changed files)
        run: pnpm check:api-route-errors

      - name: Reject secrets (PR diff)
        if: github.event_name == 'pull_request'
        run: pnpm check:no-secrets

      - name: Reject secrets (full scan)
        if: github.event_name != 'pull_request'
        run: pnpm check:no-secrets:full

      - name: Dependency usage audit
        run: pnpm deps:audit

      - name: High-severity dependency audit
        run: pnpm audit --audit-level high

      - name: Architecture boundary checks
        run: pnpm boundary:check

      - name: Reject new Domain → Lib/Infra imports
        run: pnpm check:no-new-domain-infra-imports

      - name: AI tool guardrails check
        run: pnpm ai-tools:check

      - name: Dependency cycle check (AI agents)
        run: pnpm deps:cycles

      - name: Production build
        run: pnpm build
        env:
          # Stub required env vars for build (no secrets needed)
          NEXT_PUBLIC_SITE_URL: 'https://example.com'
          NEXT_PUBLIC_SUPABASE_URL: 'https://abcd1234.supabase.co'
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: 'sb_publishable_dummy'

      - name: Unit tests (affected)
        env:
          VITEST_POOL: threads
        run: pnpm test:affected

      - name: Determine changed files
        id: changed_files
        run: |
          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            base_ref="${{ github.base_ref }}"
            git fetch --no-tags origin "$base_ref"
            changed_files="$(git diff --name-only "origin/${base_ref}...HEAD")"
          else
            before="${{ github.event.before }}"
            if [ -n "$before" ] && [ "$before" != "0000000000000000000000000000000000000000" ]; then
              git fetch --no-tags --depth=1 origin "$before"
              changed_files="$(git diff --name-only "$before" HEAD)"
            else
              changed_files="$(git ls-tree -r --name-only HEAD)"
            fi
          fi

          {
            echo "files<<EOF"
            printf '%s\n' "$changed_files"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Determine if critical E2E needed
        id: critical_e2e
        env:
          CHANGED_FILES: ${{ steps.changed_files.outputs.files }}
        run: |
          if printf '%s\n' "$CHANGED_FILES" | grep -Eq '^(e2e/|playwright\.config\.ts|scripts/e2e-webserver\.mjs|src/(app|components|features|hooks|lib|server|stores)/.*|package\.json|pnpm-lock\.yaml|pnpm-workspace\.yaml)$'; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Playwright Chromium (critical E2E)
        if: steps.critical_e2e.outputs.run == 'true'
        run: pnpm exec playwright install --with-deps chromium

      - name: Critical E2E smoke (chromium)
        if: steps.critical_e2e.outputs.run == 'true'
        run: pnpm test:e2e:critical

      - name: Determine if prod CSP check needed
        id: prod_csp
        env:
          CHANGED_FILES: ${{ steps.changed_files.outputs.files }}
        run: |
          if printf '%s\n' "$CHANGED_FILES" | grep -Eq '^(src/proxy\.ts|src/app/layout\.tsx|src/instrumentation-client\.ts|e2e/csp-nonce\.spec\.ts|playwright\.prod\.config\.ts|scripts/e2e-webserver-prod\.mjs|package\.json|pnpm-lock\.yaml|pnpm-workspace\.yaml|next\.config\.ts)$'; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Playwright Chromium (prod CSP)
        if: steps.prod_csp.outputs.run == 'true'
        run: pnpm exec playwright install --with-deps chromium

      - name: Prod CSP E2E (chromium)
        if: steps.prod_csp.outputs.run == 'true'
        run: pnpm exec playwright test -c playwright.prod.config.ts
        env:
          E2E_SKIP_BUILD: "1"

      - name: Summary
        run: |
          echo "## Frontend CI Summary" >> $GITHUB_STEP_SUMMARY
          echo "Biome lint, TS check, and unit tests completed." >> $GITHUB_STEP_SUMMARY

  frontend-coverage:
    name: Frontend Coverage (sharded)
    needs: frontend
    if: >-
      github.event_name == 'push' && startsWith(github.ref, 'refs/heads/main')
      || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
        total: [4]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v7.0.0
        with:
          node-version-file: '.nvmrc'

      - name: Install deps
        run: |
          npm install -g "corepack@${COREPACK_VERSION}"
          corepack enable pnpm
          corepack install
          pnpm ci

      - name: Cache Vite/Vitest/TS caches
        uses: ./.github/actions/cache-vite-vitest

      - name: Run sharded coverage (api+ui)
        env:
          CI: '1'
          VITEST_COVERAGE_SKIP_THRESHOLDS: '1'
        run: >-
          pnpm exec vitest run --coverage
          --pool=threads --reporter=blob
          --shard=${{ matrix.shard }}/${{ matrix.total }}

      - name: Upload shard artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: vitest-reports-${{ matrix.shard }}
          path: .vitest-reports/*.json
          if-no-files-found: error
          include-hidden-files: true

  frontend-coverage-merge:
    name: Frontend Coverage (merge)
    needs: frontend-coverage
    if: ${{ needs.frontend-coverage.result == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v7.0.0
        with:
          node-version-file: '.nvmrc'

      - name: Install dependencies
        run: |
          npm install -g "corepack@${COREPACK_VERSION}"
          corepack enable pnpm
          corepack install
          pnpm ci

      - name: Download shard artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          path: .vitest-reports
          pattern: vitest-reports-*
          merge-multiple: true

      - name: Merge reports
        run: |
          pnpm exec vitest run --merge-reports .vitest-reports --coverage --passWithNoTests
          node scripts/check-coverage-critical.mjs

      - name: Upload coverage report
        uses: actions/upload-artifact@v7.0.1
        with:
          name: coverage-merged
          path: coverage
          if-no-files-found: error
